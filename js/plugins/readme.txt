1.有demo的组件:
    accordion(抽屉菜单)  buttongroup(按钮组)  confirm(确认提示)  tree(树) dialog(对话框) editor(封装kindeditor) grid(表格)
    emoticon(表情)  fancybox(图片浏览)  form(ajax表单提交)  icolorpicker(颜色选择器)  imagepreview(图片预览)  less(动态css语言)
    menubar(ligerui菜单栏)  msgbox(提示框,仿照QQ空间中)  navbar(导航栏) print(js打印)  reply(回复)  select(选择组件)
    swfupload(flash上传)  tab(多tab页) tip(表单提示)  validationengine(表单校验)  cluetip(提示)  combobox(下拉框)
    cookie(js对cookie操作)  hotkeys(热键,快捷键)  marquee(图片滚动)  switch(类似iphone开关)  scroll(滚动条美化)  menu(右键菜单)
    ieBug(特效兼容ie)  dateeditor(日期选择) drag(拖动)  resizable(改变大小)  jTemplates(js模板引擎)  poshytip(提示)
    jcrop(图片裁剪)  imglazyload(图片懒加载)  template(js模板引擎)

2.无demo的组件:

3.自己开发的:
    confirm  emoticon  msgbox  navbar  reply  swfupload  tip  editor

4.修改开源组件:
    buttongroup  imagepreview  select  ieBug